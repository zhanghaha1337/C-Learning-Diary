## 今天搞懂的核心：动态内存到底是什么？
以前用的数组都是固定大小的，比如 char str[10]，编译的时候就定死了，用不完浪费，不够用又报错。动态内存就是反过来：程序跑起来的时候，再跟系统要内存，要多少用多少，用完手动还回去。
动态内存存在堆里，不是栈里
核心函数都在 <stdlib.h> 里
用完不还就是「内存泄漏」，程序越跑越卡
## malloc 函数：我今天的重点

1. 它到底干了啥？
就是个「堆内存申请工具」，给它要的字节数，它给你一块堆内存的地址。

// 函数原型
void *malloc(size_t size);
参数：要申请的字节数，比如要 5 个 int，就是 5 * sizeof(int)
返回值：成功给你地址，失败给 NULL（必须判断！）
重点：它给的内存是脏的，里面全是随机垃圾值，不会帮你清 0
 我写的标准用法

#include <stdio.h>
#include <stdlib.h>

int main() {
    // 申请能放5个int的内存
    int *arr = (int *)malloc(5 * sizeof(int));

    // 必写！判断申请成功没
    if (arr == NULL) {
        printf("内存不够，申请失败了\n");
        return 1;
    }

    // 像数组一样用就行
    for (int i = 0; i < 5; i++) {
        arr[i] = i + 1;
    }

    // 用完必须释放
    free(arr);
    // 释放后指针置空，防止野指针
    arr = NULL;

    return 0;
}

## 踩过的坑：今天差点崩溃的教训
空间不够，硬写越界比如 char str2[] = "abc"; 只有 4 个字节，用 strcat 硬拼别的字符串，就算当时看起来拼接成功了，程序跑完也会报错，因为把栈内存写坏了。解决办法：申请的时候就给够空间，别省那点字节。
忘了判断返回值以前写 malloc 不判断 NULL，申请失败后直接操作指针，程序直接闪退，找半天 bug 才发现是这个问题。
free 后不置空指针释放内存后，指针还指着原来的地址，变成「野指针」，后面不小心用了就会出各种莫名其妙的问题。养成习惯：free(arr); arr = NULL;
malloc 不初始化申请到的内存里全是乱码，直接用会出问题，要么自己赋值，要么用 memset 清 0，或者用 calloc。

## 总结
malloc 就是跟堆要内存，给它字节数，它给你地址
必须判空、必须释放、释放后必须置空
空间一定要算够，越界写内存就算不报错，也会偷偷搞坏程序
动态内存虽然灵活，但也很容易踩坑，一步错就全乱了
