# C语言顺序表（动态数组）

## 完整代码

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <stdbool.h>

bool initList(struct list* list);//initList返回布尔值,1为正确0为错误


typedef int E;//直接修改int即可改变数据类型

struct list{
	E* array;
	int capacity;//定义数组的元素长度
	int size;//定义这个数组现在有几个元素
};


bool initList(struct list *list) {

	list->capacity = 10;//
	list->array = malloc(sizeof(E) * list->capacity);//开辟一个空间大小为单个E数据类型*capacity
	if (list->array == NULL)//开辟空间为null即为失败
	{
		return 0;
	}
	list->size = 0;
	return 1;
}


bool insertList(struct list* list,E element,int index) {
	if (index < 1 || index > list->size + 1) //判断
	{
		return 0;
	}
	if (list->size == list->capacity)//判断是否超出数组元素
	{
		int newCapacity = list->capacity + (list->capacity >> 1);//扩容
		E * newArray = realloc(list->array, newCapacity * sizeof(E));
		if (newArray == NULL)//判断是否扩容成功
		{
			return 0;
		}
		list->array = newArray;
		list->capacity = newCapacity;
	}

	for (int i = list->size; i > index -1; --i)//i为当前数组里的元素个数  ,index为在哪个索引添加,element为添加的元素
	{
		list->array[i] = list->array[i - 1];//从数组最后一个索引开始往后依次移动一位直到i > index-1

	} 
	list->array[index - 1] = element;
	
	list->size++;

	return 1;
}

bool deleList(struct list* list, int index) {

	if (index < 1 || index > list->size ) //判断index是否合法
	{
		return 0;
	}
	
	for (int i = index -1 ; i < list->size - 1; i++)
	{
		list->array[i] = list->array[i + 1];// 依次覆盖前一个元素

		
	}
	list->size--;//删除替换的元素
	return 1;
}

void printfList(struct list* list) {
	
	for (int i = 0; i < list->size; i++)
	{
		printf("%d",list->array[i]);
		printf("\n");
	}
}

int main() {

	struct list mylist;
	
	if (initList(&mylist)) {

		for (int i = 1; i < 11; i++)
		{
			insertList(&mylist, i * 10, mylist.size + 1);
			
		}

		printfList(&mylist);

		deleList(&mylist, 1);

		printf("----------------------\n");

		printfList(&mylist);

		printf("%d", mylist.size);
	}
	else
	{
		printf("顺序表初始化失败,无法启动");
	}

}
```

---

## 核心知识

### 顺序表结构

```c
struct list{
	E* array;
	int capacity;
	int size;
};
```

### array

动态数组指针

```c
E* array;
```

用于指向malloc申请的动态内存空间。

---

### capacity

数组最大容量。

```c
int capacity;
```

---

### size

当前有效元素个数。

```c
int size;
```

---

## malloc动态内存申请

```c
list->array = malloc(sizeof(E) * list->capacity);
```

作用：

申请：

```text
sizeof(E) * capacity
```

大小的连续内存空间。

如果：

```c
E = int
capacity = 10
```

则：

```text
4 * 10 = 40字节
```

---

## realloc扩容

```c
E * newArray = realloc(list->array, newCapacity * sizeof(E));
```

作用：

重新申请更大的连续内存空间。

---

## 插入元素原理

插入元素时：

后面的元素整体后移。

```text
[10][20][30]
```

插入：

```text
15
```

变成：

```text
[10][15][20][30]
```

---

## 删除元素原理

删除元素时：

后面的元素整体前移覆盖。

```text
[10][20][30][40]
```

删除：

```text
20
```

变成：

```text
[10][30][40]
```

---

## 扩容逻辑

```c
int newCapacity = list->capacity + (list->capacity >> 1);
```

作用：

扩容1.5倍。

例如：

```text
10 -> 15
15 -> 22
22 -> 33
```

---

## 注意点

### 当前代码使用的是1-based索引

即：

```text
第1个元素
第2个元素
第3个元素
```

因此：

```c
index - 1
```

用于转换成数组真实下标。

---

### 删除和插入都需要判断index是否合法

插入：

```c
index < 1 || index > size + 1
```

删除：

```c
index < 1 || index > size
```

---
